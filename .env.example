# MCP Auth Configuration
# Set these environment variables to enable OAuth/OIDC authentication for your MCP server

# OAuth/OIDC Issuer URL (required to enable auth)
# This is the base URL of your OAuth/OIDC provider
# Examples:
# - Auth0: https://your-tenant.auth0.com/
# - Keycloak: https://your-keycloak.com/realms/your-realm
# - Google: https://accounts.google.com
# - Azure AD: https://login.microsoftonline.com/your-tenant-id/v2.0
OAUTH_ISSUER_URL=

# OAuth Server Type (optional, defaults to 'oidc')
# Supported values: 'oauth', 'oidc'
OAUTH_SERVER_TYPE=oidc

# Required scopes for MCP endpoints (optional, comma-separated)
# These scopes will be required for accessing protected MCP endpoints
# Default: 'mcp:read,mcp:write'
REQUIRED_SCOPES=mcp:read,mcp:write

# Additional environment variables for wasmtime
# Example: TEST=example